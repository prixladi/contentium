import { ResourceOptions } from 'adminjs';

const options: ResourceOptions = {
  properties: {
    id: {
      isVisible: false,
    },
    value: {
      type: 'textarea',
    },
  },
} as ResourceOptions;

export default options;
